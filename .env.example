# Copy to `.env` and fill in (optional; you can also `export` in the shell).
# Never commit real secrets.

# Required for `python -m data_generator ai-schema`
OPENAI_API_KEY=

# Optional default model when `--model` is omitted
# OPENAI_MODEL=gpt-4o-mini
