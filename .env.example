# Copy to `.env` in the project root (same folder as `requirements.txt`).
# That file is gitignored. The app loads it automatically via python-dotenv when you
# use AI schema features (no need to export in the shell unless you prefer to).

OPENAI_API_KEY=

# Optional default model when `--model` is omitted
# OPENAI_MODEL=gpt-4o-mini
